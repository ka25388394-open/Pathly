# Render-compatible dependencies (no Rust compilation)
fastapi==0.100.1
uvicorn==0.23.2
pydantic==1.10.12
sqlalchemy==1.4.49
python-dotenv==1.0.0
httpx==0.24.1

# Production database
psycopg2-binary==2.9.7

# Optional (remove if causing issues)
# google-generativeai==0.3.0
