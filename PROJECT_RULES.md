# Pathly 專案管理規則

## 🚫 **絕對禁止的操作**

1. **手動指定端口啟動服務**
   ```bash
   ❌ python -m uvicorn app.main:app --port 8007  # 禁止！會忽略配置
   ❌ uvicorn app.main:app --port 8006              # 禁止！會造成混亂
   ```

2. **直接殺死服務進程**
   ```bash
   ❌ taskkill /F /PID xxxx  # 危險！可能誤殺其他服務
   ```

3. **忽略端口衝突警告**
   ```bash
   ❌ 看到端口被占用還強制啟動 # 會造成多重實例
   ```

## ✅ **必須遵循的操作**

### 1. **唯一啟動方式**
```bash
# 只能用這種方式啟動
python start_pathly.py
```

### 2. **端口配置管理**
- 端口設定只能在 `.env` 文件中修改
- 修改後重啟服務才生效
- 前端 API 設定必須與 .env 一致

### 3. **衝突處理流程**
1. 啟動前自動檢測端口衝突
2. 提示用戶選擇處理方式
3. 確認身份後才停止舊服務

### 4. **服務管理**
```bash
# 檢查服務狀態
python check_pathly_status.py

# 清理所有實例
python cleanup_services.py

# 標準啟動
python start_pathly.py
```

## 📋 **端口規範**

| 用途 | 端口 | 說明 |
|------|------|------|
| 開發環境 | 8006 | 主要開發端口 |
| 測試環境 | 8080 | 專用測試 |
| 生產環境 | 5000 | 部署端口 |

## 🔒 **安全檢查清單**

啟動服務前必須確認：
- [ ] 在正確的專案目錄中
- [ ] .env 文件存在且正確
- [ ] 前端 API 設定與 .env 一致  
- [ ] 沒有其他 Pathly 實例運行
- [ ] 使用標準啟動腳本

## 🚨 **違規懲罰**

如果不遵循規則：
1. **服務混亂** - 多重實例，端口衝突
2. **前後端不一致** - API 調用失敗
3. **CORS 問題** - 跨域請求被拒絕
4. **調試困難** - 不知道改動在哪個實例

## 📞 **問題處理**

遇到端口問題時：
1. **停止所有服務**: `python cleanup_services.py`
2. **檢查配置一致性**: 對比 .env 和前端設定
3. **標準重啟**: `python start_pathly.py`
4. **驗證功能**: 測試前端連接